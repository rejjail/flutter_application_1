name: flutter_application_1
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # Firebase dependencies (dipindah ke sini dan diperbarui)
  firebase_core: ^2.27.0
  firebase_auth: ^4.17.3
  firebase_auth_web: ^5.8.13 # Ditambahkan untuk kompatibilitas

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true